[tool.poetry]
name = "repl_python3_textadventure"
version = "0.1.0"
description = ""
authors = ["MajaKerste <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
tabulate = "^0.8.9"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
